# samples/

Jeux synthetiques pour valider le pipeline sans telecharger NCBI.
Ne pas confondre avec de vrais genomes : prefixe `SYNTH_`.

- `synth_demo.fasta` : 3 sequences courtes pour tests QC.
Les vrais FASTA Puumala iront dans `data/raw/` (gitignored) via `io_ncbi.py`.
